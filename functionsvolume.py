# define function volume
def volume(l,w,h):
    #print the volume of the cuboid
    print (" The volume is \n" , l * w * h)

#prompt user to enter values form keyboard
l = float (input("Enter value of length\n"))
w= float(input("Enter value of width\n"))
h= float(input("Enter value of height\n"))

#call/invoke the function name for function to execute
volume(l, w, h)


